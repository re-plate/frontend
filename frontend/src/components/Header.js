import React from "react";

const Header = () => {
  return (
    <div className="header">
      <h1>Donate to Replate</h1>
      <button className="requestButton">View open requests</button>
    </div>
  );
};

export default Header;
